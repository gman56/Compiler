f(a : int, b : int) : bool
{
        if (a != b)
                then return a < b
                else return a - b
}       


main() : int
{
        if (f(10, 20))
                then print 1
                else print 0
        return 0        
}
